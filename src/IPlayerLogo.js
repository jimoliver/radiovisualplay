import { Typography } from "@mui/material";

export default function IPlayerLogo({ fontSize='4rem' }) {
    return (
        <Typography fontSize={fontSize} color={'#f54997'} marginLeft={5} marginTop={3}>iPLAYER</Typography>
    );
}